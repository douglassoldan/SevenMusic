import { StyleSheet } from 'react-native'
import colors from '../../styles/colors'

export default StyleSheet.create({
    simpleInput: {
        borderColor: "#000",
        borderWidth: 2,
        backgroundColor: "red"
    }
})